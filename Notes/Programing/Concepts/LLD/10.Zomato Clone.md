will take it as an interview => will design and ask requirements.
## Design a food delivery app -> Tomato
Functional requirements(product/business related)
- user can search for restaurants based on location.
- user can add items to carts.
- user can checkout by making payments
- user should be notified once order is placed successfully
Non-functional requirements(general related)
- each part of design should be modifiable
will not make it in 1 hr => do only flow and UML, design principles and patter, working code(structural).
![[Pasted image 20260926123654.png]]
can add items in a cart
if have 2 types of order => delivery order or pickup order(payment).
for payment mostly will use a 3rd party service => as main focus of app is food delivery.
2 parts = user + delivery agent;
cart => hold collection of item.
Happy flow : ![[Pasted image 20260926124542.png]]
## UML diagram
will understand at code level => will bring interview on same page.
will lead to conclusion => tread as interviewer as friend and ask advice and question.
Do it -> Bottom-up / top-down approach.
make small object => big objects 
make big object => small objects
better will be bottom up(make smaller => bigger).
which object can be there in this app.
![[Pasted image 20260926140628.png]]
`menuItem` - restaurant has composition(strict composition) relation ![[Pasted image 20260926141027.png]]
need some object to manager the restaurants. => main is search by location.![[Pasted image 20260926143933.png]]
restaurant manager is aggregation of restaurants.
![[Pasted image 20260926144220.png]]
as to make it loss-ly coupled make talk to restaurant via manager instead of directly.
all managers -> singleton class.
now, for users => cart (strict composition).
![[Pasted image 20260926145336.png]]
for payment -> use strategies design pattern.
![[Pasted image 20260926150632.png]]
order object -> ![[Pasted image 20260926151246.png]]
order has simple association with all other objects.
`orderFactory` and `orderManager`.
order creation for now and for later(schedule).
![[Pasted image 20260926154346.png]]
now need order manager(single point of contact for all order related queries).
finally need a notification -> will probability do 3rd party service(don't make everything from scratch).
![[Pasted image 20260926155732.png]]
now, need make class -> main class(orchestra class) to provide single point of contact to the client.
client should be unaware of all our internal classes all this delegated by the orchestra class(break `SRP` and rule of minimum knowledge).
full diagram
![[Pasted image 20260926160248.png]]