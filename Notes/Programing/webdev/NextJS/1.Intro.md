###### Can make web-pages easily using next, as boiler plate of configuration, linting, compilation and compilation
###### Next JS make this process stream line and easy => in 2013 we can make react
###### React also suggest to use a full stack framework such as NEXTJS
### what next JS have which react doesn't ?!
###### => It simplifies and optimizes app by default
###### => division of components as server and client side components
###### =>As server components are more performer So, all components are by default server component
###### =>Advance rendering , choose where to render client and server side rendering
###### => file routing no other module to install-> each folder make a route
###### =>automatic code splitting(only current page code is loaded), scaling, font optimization(to avoid layout shift), Image optimization(Lazy loading etc), script optimization. => all this optimization by default