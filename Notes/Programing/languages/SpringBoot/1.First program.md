make a simple server which will response.
![[Pasted image 20260627231027.png]]
every device has own IP address.
https://whatismyipaddress.com/
if a client want to send info to this computer => send on this IP address.
URL are `www.example.in` -> this is resolved to an IP by DNS resolver.
![[Pasted image 20260627231640.png]]
localhost -> 127.0.0.1(self request)
many apps run on a device which need to send and receive data via internet. => if data come to devices IP address how to decide whose it is => use port on the IP. => can have many ports one one IP address.
Port number -> unique apps run on one port.
thus request also contains port number.
There, are know standard port for app => 
HTTP : 80
HTTPS : 443
if server is running on server 8080
Reverse Proxy : it maps port request.
![[Pasted image 20260703135047.png]]
Reverse proxy is a part of server.
## Start a server
make a hello end point for localhost respond
- java web applications needs
	- create folder structure
	- configurations
	- dependency(external library) like DB(e.g MySQL connector) => to avoid making everything from scratch.
	- Spring boot version config, java version config
	- package structure
- then, finally do business logic
can do directly using [Spring Initializr](https://start.spring.io/index.html)
![[Pasted image 20260703140555.png]]
snapshot => under-development version.
RC1 => will release yet not released.
go and do stable version
JAR : java archives
WAR : web archives
add dependencies => spring web.
![[Pasted image 20260703141016.png]]
![[Pasted image 20260703141923.png]]
download it.
![[Pasted image 20260703142138.png]]
in `DemoApplication.java`
```java
package com.example.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class DemoApplication {

    public static void main(String[] args) {
        SpringApplication.run(DemoApplication.class, args);
        System.err.println("Hello World");
    }

}
```
to run: 
```bash
./mvnw spring-boot:run
```

![[Pasted image 20260703144007.png]]
first time running will take time then,will be fast.
now in browser
![[Pasted image 20260703144107.png]]
made no make for this page thus, see this fallback.
make a controller for this ![[Pasted image 20260703144320.png]]
```java
package com.example.demo;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class HelloController {
    // this is to handle end points
    @GetMapping("hello")
    public String hello(){ // need to call it on end points
        return "Hello World";
    }
}
```
now in browser at /hello end point ![[Pasted image 20260703144752.png]]
simply made an endpoint.
can pass html
```java
    @GetMapping("hello")
    public String hello(){ // need to call it on end points
        return "<h1>Hello World</h1>";
    }
```
on browser![[Pasted image 20260703144958.png]]
## Observation
- from logs `Tomcat initialized with port 8080 (http)` , who started this ?
	- Have installed Tomcat from initial config
- how this is mapped to end points?
can change port from the file ![[Pasted image 20260703145327.png]]
```java
spring.application.name=demo
server.port=9090
```
![[Pasted image 20260703145413.png]]
can make multiple end points
```java
@RestController
public class HelloController {
    @GetMapping("hello")
    public String hello(){
        return "<h1>Hello World</h1>";
    }
    @GetMapping("bye")
    public String byebye(){ // need to call it on end points
        return "<h1>Bye bye</h1>";
    }
}
```
made quick app => can jump to business logic.
Hard to learn spring boot => need to learn all spring core ideas.
need to learn full architecture for it.
