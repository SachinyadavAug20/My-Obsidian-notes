###### It is rendered on server and send to the client => render the page and send the HTML output to the client to be presented directly
###### So, can to server side stuff like DB logic etc on the server and only send HTML to client so limited JS send to client and loads faster
###### But can't to everything on server side as it can't interact with browser (as it is executed on server) like can't interact with interface, button, alert etc
