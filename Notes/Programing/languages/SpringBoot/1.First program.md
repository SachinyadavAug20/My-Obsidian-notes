make a simple server which will response.
![[Pasted image 20260627231027.png]]
every device has own IP address.
https://whatismyipaddress.com/
if a client want to send info to this computer => send on this IP address.
URL are `www.example.in` -> this is resolved to an IP by DNS resolver.
![[Pasted image 20260627231640.png]]
localhost -> 127.0.0.1(self request)
many apps run on a device which need to send and receive data via internet. => if data come to devices IP address how to decide whose it is => use port on the IP. => can have many ports one one IP address.
Port number -> unique apps run on one port.
thus request also contains port number.
There, are know standard port for app => 
HTTP : 80
HTTPS : 443
if server is running on server 8080
Reverse Proxy : it maps port.
