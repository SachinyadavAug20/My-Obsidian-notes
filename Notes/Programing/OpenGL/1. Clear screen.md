### make env variable
> $ python -m venv My_neo_venv
> source .venv/bin/activate
### start up
> $ pip install PyOpenGL
> $ pip install pygame-ce 
	it is some dicriptiency btw this library dev so get -ce(cute edition)
> $ pip install numpy 