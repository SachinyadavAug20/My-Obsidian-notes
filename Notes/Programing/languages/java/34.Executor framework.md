We create thread manually, may lead to complexity.
- memory allocation
```java
Thread t1=new Thread;
t1.start();
```
each thread get `512KB` to `1MB` space.
- OS scheduling => as limited core making many threads will lead to many context switching(waste of time) lead to thrashing(switching)
- Thread creation takes time.
- doing all that to make thread for 1 time use which is waste of resources.
Thus, manual is bad approach. as like hire new chief to make new dish and then fire him.
Thus, to solve it by Executor framework
```mermaid
graph TD
    %% Main Tree Structure
    Root[Threads] --> Task["Task<br/><i>(What to do)</i>"]
    Root --> Execution["How to do?<br/><i>(Execution Mechanism)</i>"]

    %% Execution Breakdown
    Execution --> T["Thread"]
    T --> R["Runnable"]

    %% Style Formatting
    style Root fill:#dcdcdc,stroke:#333,stroke-width:1px,color:#000
    style Task fill:#1a1a00,stroke:#e6e600,stroke-width:1px,color:#e6e600
    style Execution fill:#2e3b82,stroke:#7da7d9,stroke-width:1px,color:#fff
    style T fill:#000,stroke:#fff,stroke-width:1px,color:#fff
    style R fill:#000,stroke:#fff,stroke-width:1px,color:#fff
```
java will bring a frame work (like collection framework)
it only ask what to do(Task) => no how.
It is done using new concept thread pool
## Thread pool
There are many threads called workers in this pool.
each task is assigned to thread/worker.
This thread/worker in thread pool will not terminate after task it will go to thread pool.
life cycle is managed by thread pool. => thread repeatability.
It has queue => for tasks (if thread no available will send in queue). thus fair.
```mermaid
graph LR
    %% Task Stream
    subgraph Tasks [Submitted Tasks]
        T1[Task 1]
        T2[Task 2]
        T3[Task 3]
        T_Ellipsis[...]
        T11[Task 11]
    end

    %% Queue Buffer
    subgraph Wait_Queue [Blocking Queue]
        Q_Head["T8 (Next)"]
        Q_Mid1[T9]
        Q_Mid2[T10]
        Q_Tail[T11]
    end

    %% Worker Threads
    subgraph Pool [Worker Pool]
        t1[Worker t1]
        t2[Worker t2]
        t3[Worker t3]
        t4[Worker t4]
        t5[Worker t5]
    end

    %% Connections
    Tasks -->|Pool Full| Wait_Queue
    Tasks -->|Idle Worker Available| Pool
    Wait_Queue -->|Worker becomes free| t1

    %% Styling
    style Wait_Queue fill:#fff2cc,stroke:#d6b656,stroke-width:2px
    style Pool fill:#e1f5fe,stroke:#03a9f4,stroke-width:2px
```
if queue is full => task will be discard.
There are many types of thread pool.
