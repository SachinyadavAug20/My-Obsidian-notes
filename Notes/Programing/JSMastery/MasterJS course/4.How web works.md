###### Learn fundamental to understand the web and make good and clear design decisions.
## vanilla approach
###### use HTML CSS JS =>pages where built using fundamental elements of web ![[Pasted image 20260405133338.png]]
![[Pasted image 20260405133437.png]]
###### Most processing happens on client side => take loading time bandwidth and processing power => for complex app it becomes to much for the browser to handle
## React way (by Facebook)
![[Pasted image 20260405133832.png]]
###### It send minimal HTML file as response for enter point and a bundled JS file 
![[Pasted image 20260405133923.png]]
###### It initiate client side rendering using this JS files and virtual DOM =>Instead of direct modification to real DOM it changes virtual DOM then applies the necessary changes to real DOM using a process called "diffing"
![[Pasted image 20260405134258.png]] 
###### It allows to change URL without making any request(reload) =>it only re-render the relevant components and thing not changing remain intact => if that component need data it send a request and whole page remains static
## Next JS
###### It has benefits of both can render thing on server or client =>when request comes it server send fully rendered html and send to client
![[Pasted image 20260405135003.png]]
###### This html has everything no need for JS renders quickly=> will send JS code for user interaction (client components)
## Hydration
###### hydration error = when user interface doesn't match what was made by server -> so server gives error
![[Pasted image 20260405135418.png]]
###### hydration is the process of attaching event-handlers and interactivity to pre-rendered html (where there are placeholder for it) => error happens when this placeholder doesn't match the expectation of JS hydrating it.
![[Pasted image 20260405135809.png]]
###### do npx create-next-app 
###### Package.json => has basic info about package
###### package-lock.json => has version and dependencies version of all package (for exact same match)

###### console log server side and client => server side show both client and server console log due to server side pre-render of client components