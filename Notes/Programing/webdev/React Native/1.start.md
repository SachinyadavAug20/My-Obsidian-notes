clerk -> auth and billing, which have native components for expo.
PostHog -> analytics and surveys
# React native
## Fundamentals
can render native mobile components -> like `<text>` box.
```jsx
<Text style={{font:24,color:"blue"}}>Hello world!</Text>
```
![[Pasted image 20260801143253.png]]
now `<View>` is same as box.![[Pasted image 20260801143429.png]]
it has many prob -> and uses flex box by default.
can make it ![[Pasted image 20260801143648.png]]
![[Pasted image 20260801143729.png|288]]![[Pasted image 20260801143817.png|406]]
for list map`<FlatList>`, But it has extra features like:
- Optimized scroll performance.
- item separation
```jsx
import { View, FlatList, Text } from 'react-native';

const DATA = [
  { id: '1', title: 'Item 1' },
  { id: '2', title: 'Item 2' },
  { id: '3', title: 'Item 3' },
  { id: '4', title: 'Item 4' },
  { id: '5', title: 'Item 5' },
];

const App = () => {
  return (
    <View>
      <FlatList
        data={DATA}
        renderItem={({ item }) => (
          <View>
            <Text>{item.title}</Text>
          </View>
        )}
        keyExtractor={item => item.id}
      />
    </View>
  );
};
```
The question is :
- When should you use `FlatList`?
- When should you map?
answer
- `FlatList`
	- For larger lists with smooth scrolling
- `Map` function
	- For smaller list
##### `ScrollView`
it is a container -> which is scroll-able.
```jsx
import { View, ScrollView, Text } from 'react-native';

const App = () => {
  return (
    <View>
      <ScrollView>
        <Text>Apple</Text>
        <Text>Banana</Text>
        <Text>Cherry</Text>
        <Text>Date</Text>
        <Text>Elderberry</Text>
        <Text>Fig</Text>
        <Text>Grape</Text>
        <Text>Honeydew</Text>
        <Text>Iced Tea</Text>
        <Text>Jackfruit</Text>
      </ScrollView>
    </View>
  );
};
```
##### `SafeAreaView`
it will display that content without hiding it behind the hardware features.
