we have new game load game and 3 canvas
need to switch between the three to make a good UX.
can change sort order to change which canvas is on top.
![[Pasted image 20260703190646.png]]
will disable and enable ![[Pasted image 20260703190955.png]]
need a back button to go to ![[Pasted image 20260703191509.png]]
add same on click listener for it changing boolean to false on click.
now can navigate from new game and load game.
![[Pasted image 20260703192224.png]]
## Features
need a feature to delete saved games
make UI for delete.
![[Pasted image 20260704155617.png]]
in game manager below save and load game add delete game also
```cs
    public void DeleteGame(string gameName){
        PlayerPrefs.DeleteKey(gameName);
        AllGameName.Remove(gameName);
        string commaSeparatedGameName=string.Join(",",AllGameName);
        PlayerPrefs.SetString("AllGameName", commaSeparatedGameName);
        PlayerPrefs.Save();
        SceneManager.LoadScene("menu"); // reload
    }
```
in button do this 
```cs
    public void deleteGame()=> GameManager.Instance.DeleteGame(_gameName);
```
![[Pasted image 20260704160053.png]]
It will delete it and but need to restart game or reload scene to see changes => thus reload
but it goes to menu and need to click to see changes.
make UI update => do it simply => simplicity is a key.
remove the reload and change to UI.
```cs
    public void deleteGame()
    {
        GameManager.Instance.DeleteGame(_gameName);
        Destroy(gameObject);
    }
```
don't make it complex.
## Give a conformation
don't directly delete
![[Pasted image 20260704162525.png]]
X will show up it confirm button, and confirm button will delete
cancel by UI ![[Pasted image 20260704162915.png]]
path -> ![[Pasted image 20260704163357.png|195]]->![[Pasted image 20260704163413.png|295]]...
## Bug
when there is no game this black game shows up why
![[Pasted image 20260704163657.png]]
- when all games deletes and reload the menu so should have no saved game this ghost games shows up
see in panel which creates this buttons
```cs
using UnityEngine;

public class GamesListPanel : MonoBehaviour
{
    [SerializeField] private LoadGameButton _loadGameButtonPrefab;
    private void Start(){
        foreach(var gameName in GameManager.Instance.AllGameName){
             var button = Instantiate(_loadGameButtonPrefab,transform);
             button.SetGameName(gameName);
        }
    }
}
```
add break point for debug.
it has 1 game some how which is blank "".
see where adding 
see game manager
```cs
    public List<string> AllGameName = new List<string>();
    void Awake() {
        if (Instance != null) {
            Destroy(gameObject);
            return;
        }
        Instance = this;
        DontDestroyOnLoad(gameObject);
        _playerInputManager = GetComponent<PlayerInputManager>();
        _playerInputManager.onPlayerJoined += HandlePlayerJoined;

        SceneManager.sceneLoaded += HandleSceneLoaded; // event handler
        string text=PlayerPrefs.GetString("AllGameName");
        AllGameName=text.Split(',').ToList();
        Debug.Log("AllGameName " +text);
    }
```
when gets All game names from `playerprefs` it does't check is blank=> as when gets `""` split by `,` give 1 element `""`
so fix
```cs
        string text=PlayerPrefs.GetString("AllGameName");
        if(!string.IsNullOrEmpty(text)) AllGameName=text.Split(',').ToList();
```
clean up
see size of images.
