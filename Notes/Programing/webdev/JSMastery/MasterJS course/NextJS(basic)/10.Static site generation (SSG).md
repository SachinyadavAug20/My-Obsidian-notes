###### It is technique where html pages are generated on build time (made when build time not when request comes) 
###### It is extremely fast.The result is cached and can be pushed to a content delivery network(CND)

###### For changing pages use ISR(incremental static regneration) it is extension of SSG it allows to update static content after build
![[Pasted image 20260331122336.png]] It will re-validate entire page after 1 hour 
###### Here we check if new blog add every hour and update site![[Pasted image 20260331122452.png]] This will re-validate this particular fetch only
###### Can also do on-demand re-validation check DOCS
###### This rendering strategy good for static page (changes occasionally)