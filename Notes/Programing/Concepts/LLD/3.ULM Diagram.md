What is operator overloading in C++, and why operator overloading not allowed in other language(java,python) ??
- In C++, operators are symbols that perform operations on values. like +-*/%
- for complex number different way to add show need to show it
```cpp
class Complex {
private:
    int real;
    int imag;

public:
    Complex(int r, int i) {
        real = r;
        imag = i;
    }

    // Overloading + operator
    Complex operator+(const Complex& other) {
        return Complex(real + other.real, imag + other.imag);
    }

    void display() {
        cout << real << " + " << imag << "i" << endl;
    }
};

int main() {
    Complex c1(3, 4);
    Complex c2(5, 2);
    Complex c3 = c1 + c2;
    c3.display();
    return 0;
}
```
- The same operator symbol can perform different operations depending on the types of its operands.
```cpp
10 + 20          // Integer addition
2.5 + 3.5        // Floating-point addition
c1 + c2          // Complex-number addition

// why
Distance d3 = d1.add(d2); // without operator overloading
Distance d3 = d1 + d2;    // with operator overloading
```
also same can give logic for comparision
```cpp
bool operator>(const Money& other) {
    return amount > other.amount;
}
```
The goal is not to invent random meanings for operators. The goal is to make operations on objects natural and readable.
Why isn't operator overloading allowed in Java and Python?
- Operator overloading is not completely disallowed in Java and Python.
- ![[Pasted image 20260912112358.png]]
in java
```java
Complex c3 = c1 + c2;  // Not allowed for user-defined Complex objects

// instead use
Complex c3 = c1.add(c2);
```

---
## UML diagram
UML -> unified modeling language
If have an idea of website need to show it to friend in a formal way.
- Write big paragraphs for descripting fully.(bad idea and non intutive)
- Diagramitcaly explain(how things are connected)
UML -> has 2 parts.
- Structural (static structure) -> 7
- Behavioural (Application components/ components) -> 7
total we have 14 UML diagrams.
practically use are `Class Diagram`(structural),`Sequence Diagram`(Dyanmic).
## Class Diagram
there are 2 parts 
- class structure
- association/connection.
structure block of a class -> ![[Pasted image 20260912123445.png]]
now how to show access modifer.
-> public,private,protected.![[Pasted image 20260912130426.png]]
it is shown by symbols![[Pasted image 20260912131219.png]]
will make it -> ![[Pasted image 20260912131414.png]]
if this is abstract class ![[Pasted image 20260912133319.png]]![[Pasted image 20260912134506.png]]
now need to show association of this classes.
#### Associations
there are 2 types of associatiom  ![[Pasted image 20260912143516.png]]
all 3 types of object association is collectively called as composition. -> they are programatically same but slight logical difference.
##### Class Association
inheritance -> `IS-A` relationship.
```cpp
class A {
    method1();
}

class B : public A {
    method2();
}

main() {
    B * b = new B();
    b->method1();
    b->method2();
}
```
![[Pasted image 20260912162625.png]] show by closed(tip of arrow is closed) arrow
##### Composition
it is `Has-a` relationship.-> in all 3.
###### Simple Association
It is simplest association possible.
example : ![[Pasted image 20260912155204.png]] only simple link.
shown by open arrow![[Pasted image 20260912155502.png]] -> ![[Pasted image 20260912155629.png]]
###### Aggregation
complex related -> main object acts as container for all minor objects.
example : Room -> has-a chain,bed,chair,sofa.
stronger than simple, and weaker than association.
represented by diamond (opposite direction than of `Has-A`)![[Pasted image 20260912160254.png]]

> [!NOTE] Note
> Sofa,bed,chair are part of room but they can exist independently.

###### Association
It is proper association,strongest relation between objects.
example: chair has wheels,arms,seat.
represented by filled diamond ![[Pasted image 20260912161031.png]]
> [!NOTE] Note
> minor can't exist independently

all composition is `Has-A` relation not `Is-A` relation.
Code wise -> `Has-A` is using a member of it.
done by construction injection.
```cpp
class A {
    method1();
}

class B {
    A * a;
    B() { a = new A(); }
    method2(){
    }
}
```
to run `method1` need to use A from B and use it.
```cpp
main() {
    B * b = new B(new A);
    b->method2();
    b->a->method1();
}
```
in LLD
- composition is very used relation.
- more used than inheritance

Example: Car -> manual and electric => inheritance relation.
![[Pasted image 20260912165557.png]]
sometime difference between 3 association ->> then, it is subjective matter(no correct answer).
example : restaurant -> menu relation is subjective (aggregation or composition).

---
## Sequence diagram
it shows behavior, how different classes interact/communication with each other.
components of sequence diagram -> it don't show full class member and method.(class structure don't matter here only interaction matters).![[Pasted image 20260913122154.png]] this is object A,B,C and D.
terms
- Lifeline -> life cycle or time of existence of that component.![[Pasted image 20260913122241.png]] A and B exist are same as lifeline of the application.![[Pasted image 20260913122314.png]]
- Activation Bar -> when that component was active.![[Pasted image 20260913122400.png]], in-active means object can't receive or give message. 
- Messages -> 2 types of messages ![[Pasted image 20260913122738.png]], sync(wait for response), async(no waiting).![[Pasted image 20260913123016.png]] sync message(closed tip arrow),![[Pasted image 20260913123138.png]] async message(open tip arrow).
- create message, destroy message -> some object/entity/component will be created or destroyed during the lifeline of application -> to create and destroy this message is send.![[Pasted image 20260913124418.png]]
- lost and found message -> message is lost(not reached other object ex receiver was not active)(i.e lost(receiver is unknown) and found(source is unknown)) ![[Pasted image 20260913125241.png]]
### Examples
how to draw ?
1. user uses ATM ![[Pasted image 20260913125825.png]]![[Pasted image 20260913130314.png]] make Sequence diagram for it
things to know before sequence diagram
- use-case/flow should be clear
- object involved in the flow.
![[Pasted image 20260913130427.png]]
Draw sequence diagram.
![[Pasted image 20260913130527.png]]
observation
- user,ATM active throughout.
- transaction is created when `withdraw` message comes(create message).
- `return true` is destroy message for transaction.
- account is activated by the transaction for checks message exchange sync message and response.
- cash dispenser is activated only when confirmed we need to dispense cash.
there can be many sequence diagram for an application.(as for each user-case/use/flow there is unique sequence diagram).
other terms
- alt -> if else, if there balance insufficient then alt flow ![[Pasted image 20260913131154.png]](this used for alternative flow i.e other than happy flow)
- option -> if 
- loop -> loop/iteration.

